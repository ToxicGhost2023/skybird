import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        peykan: ["var(--font-peykan)"],
      },
    },
  },
};

export default config;
